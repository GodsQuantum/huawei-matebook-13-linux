diff --git a/data/CMakeLists.txt b/data/CMakeLists.txt
index 31528d544f7bc0b8bcb3973a308d75e403c832df..417f5131333d7eab925883b14f32c517c6524bd4 100644
--- a/data/CMakeLists.txt
+++ b/data/CMakeLists.txt
@@ -60,4 +60,7 @@ if(INSTALL_PAM_CONFIGURATION)
     install(FILES pam/${PAM_OS_CONFIGURATION}/plasmalogin-autologin DESTINATION ${PAM_CONFIG_DIR})
     install(FILES pam/${PAM_OS_CONFIGURATION}/plasmalogin-greeter DESTINATION ${PAM_CONFIG_DIR})
     install(FILES pam/${PAM_OS_CONFIGURATION}/plasmalogin DESTINATION ${PAM_CONFIG_DIR})
+    if(PAM_OS_CONFIGURATION STREQUAL "arch")
+        install(FILES pam/arch/plasmalogin-fingerprint DESTINATION ${PAM_CONFIG_DIR})
+    endif()
 endif()
diff --git a/data/pam/arch/plasmalogin b/data/pam/arch/plasmalogin
index 9fad59d3845a72066ef597a403e6c93f713fd95b..af50d57292d8160c95b1c6d964df235d66b47f93 100644
--- a/data/pam/arch/plasmalogin
+++ b/data/pam/arch/plasmalogin
@@ -3,7 +3,6 @@
 # SPDX-License-Identifier: CC0-1.0
 # SPDX-FileCopyrightText: none
 
-auth        sufficient  pam_fprintd.so max-tries=3 timeout=12
 auth        include     system-login
 -auth       optional    pam_gnome_keyring.so
 -auth       optional    pam_kwallet5.so
diff --git a/data/pam/arch/plasmalogin-fingerprint b/data/pam/arch/plasmalogin-fingerprint
new file mode 100644
index 0000000000000000000000000000000000000000..376300866d507d58a0abae330e584b306c3c0666
--- /dev/null
+++ b/data/pam/arch/plasmalogin-fingerprint
@@ -0,0 +1,12 @@
+#%PAM-1.0
+
+auth       required     pam_shells.so
+auth       requisite    pam_nologin.so
+auth       requisite    pam_faillock.so preauth
+-auth      required     pam_fprintd.so max-tries=3 timeout=12
+auth       optional     pam_permit.so
+auth       required     pam_env.so
+
+account    include      system-local-login
+password   required     pam_deny.so
+session    include      system-local-login
diff --git a/src/auth/Auth.cpp b/src/auth/Auth.cpp
index 41dcb16bdeed6d752f771f006aef8261fadaa657..00c20dc5539d7c0bc0a0ab9488af66591159149f 100644
--- a/src/auth/Auth.cpp
+++ b/src/auth/Auth.cpp
@@ -58,6 +58,7 @@ public:
     QLocalSocket *socket{nullptr};
     QString sessionPath{};
     QString user{};
+    QString pamService{};
     bool autologin{false};
     bool greeter{false};
     QProcessEnvironment environment{};
@@ -297,6 +298,11 @@ const QString &Auth::user() const
     return d->user;
 }
 
+const QString &Auth::pamService() const
+{
+    return d->pamService;
+}
+
 bool Auth::verbose() const
 {
     return d->child->processChannelMode() == QProcess::ForwardedChannels;
@@ -354,6 +360,11 @@ void Auth::setSession(const QString &path)
     }
 }
 
+void Auth::setPamService(const QString &service)
+{
+    d->pamService = service;
+}
+
 void Auth::setVerbose(bool on)
 {
     if (on != verbose()) {
@@ -383,6 +394,9 @@ void Auth::start()
     if (d->greeter) {
         args << QStringLiteral("--greeter");
     }
+    if (!d->pamService.isEmpty()) {
+        args << QStringLiteral("--pam-service") << d->pamService;
+    }
     d->child->start(QStringLiteral("%1/plasmalogin-helper").arg(QStringLiteral(LIBEXEC_INSTALL_DIR)), args);
 }
 
diff --git a/src/auth/Auth.h b/src/auth/Auth.h
index 1e97de94d4b0da8c001524e66ff9d02de3d45854..1db38f7703ef812e729157795ad74d9e77e792f7 100644
--- a/src/auth/Auth.h
+++ b/src/auth/Auth.h
@@ -79,6 +79,7 @@ public:
     bool verbose() const;
     const QString &user() const;
     const QString &session() const;
+    const QString &pamService() const;
     AuthRequest *request();
     /**
      * True if an authentication or session is in progress
@@ -130,6 +131,7 @@ public:
      * @param path Path of the session executable to be started
      */
     void setSession(const QString &path);
+    void setPamService(const QString &service);
 
 public Q_SLOTS:
     /**
diff --git a/src/common/Messages.h b/src/common/Messages.h
index 94a085629d49f91c036b34028649df6f8691a160..063592c4de84babb71356b8f5308aa1deef0df20 100644
--- a/src/common/Messages.h
+++ b/src/common/Messages.h
@@ -24,12 +24,15 @@ namespace PLASMALOGIN
 enum class GreeterMessages {
     Connect = 0,
     Login,
+    FingerprintLogin,
+    CancelLogin,
 };
 
 enum class DaemonMessages {
     LoginSucceeded,
     LoginFailed,
     InformationMessage,
+    LoginCancelled,
 };
 }
 
diff --git a/src/daemon/Display.cpp b/src/daemon/Display.cpp
index 92feddb249b0acec9dfcc1f6467f4872676914bc..c41371ed0df9b86d6744161cee1a64d89105bf35 100644
--- a/src/daemon/Display.cpp
+++ b/src/daemon/Display.cpp
@@ -107,8 +107,10 @@ Display::Display(Seat *parent)
     connect(m_auth, &Auth::info, this, &Display::slotAuthInfo);
     connect(m_auth, &Auth::error, this, &Display::slotAuthError);
 
-    // connect login signal
+    // connect authentication requests from the greeter.
     connect(m_socketServer, &SocketServer::login, this, &Display::login);
+    connect(m_socketServer, &SocketServer::fingerprintLogin, this, &Display::fingerprintLogin);
+    connect(m_socketServer, &SocketServer::cancelLogin, this, &Display::cancelLogin);
 
     // connect login result signals
     connect(this, &Display::loginFailed, m_socketServer, &SocketServer::loginFailed);
@@ -256,11 +258,30 @@ void Display::login(QLocalSocket *socket, const QString &user, const QString &pa
         return;
     }
 
-    // authenticate
+    // Password authentication always uses the normal plasmalogin PAM stack.
     startAuth(user, password, session);
 }
 
-bool Display::startAuth(const QString &user, const QString &password, const Session &session)
+void Display::fingerprintLogin(QLocalSocket *socket, const QString &user, const Session &session)
+{
+    m_socket = socket;
+    startAuth(user, QString(), session, QStringLiteral("plasmalogin-fingerprint"));
+}
+
+void Display::cancelLogin(QLocalSocket *socket)
+{
+    if (socket != m_socket || !m_auth->isActive()) {
+        m_socketServer->loginCancelled(socket);
+        return;
+    }
+
+    m_authCancellationPending = true;
+    m_cancelSocket = socket;
+    m_socket = nullptr;
+    m_auth->stop();
+}
+
+bool Display::startAuth(const QString &user, const QString &password, const Session &session, const QString &pamService)
 {
     qDebug() << "start auth" << "user" << session.isValid() << session.exec();
 
@@ -338,6 +359,7 @@ bool Display::startAuth(const QString &user, const QString &password, const Sess
     env.insert(QStringLiteral("XDG_SESSION_DESKTOP"), session.desktopNames());
 
     m_auth->setUser(user);
+    m_auth->setPamService(pamService);
     if (m_reuseSessionId.isNull()) {
         m_auth->setSession(session.exec());
     }
@@ -400,6 +422,15 @@ void Display::slotAuthError(const QString &message, Auth::Error error)
 
 void Display::slotHelperFinished(Auth::HelperExitStatus status)
 {
+    if (m_authCancellationPending) {
+        m_authCancellationPending = false;
+        if (m_cancelSocket) {
+            m_socketServer->loginCancelled(m_cancelSocket);
+            m_cancelSocket = nullptr;
+        }
+        return;
+    }
+
     // Don't restart greeter and display server unless plasmalogin-helper exited
     // with an internal error or the user session finished successfully,
     // we want to avoid greeter from restarting when an authentication
diff --git a/src/daemon/Display.h b/src/daemon/Display.h
index bdfa1cd35f0b19d177daa93b7e4023f224d837c5..30cff3644c12e6c730e6db0f9d0d611cd80c951e 100644
--- a/src/daemon/Display.h
+++ b/src/daemon/Display.h
@@ -59,6 +59,8 @@ public slots:
     void stop();
 
     void login(QLocalSocket *socket, const QString &user, const QString &password, const Session &session);
+    void fingerprintLogin(QLocalSocket *socket, const QString &user, const Session &session);
+    void cancelLogin(QLocalSocket *socket);
     void displayServerStarted();
 
 signals:
@@ -69,12 +71,14 @@ signals:
     void loginSucceeded(QLocalSocket *socket);
 
 private:
-    bool startAuth(const QString &user, const QString &password, const Session &session);
+    bool startAuth(const QString &user, const QString &password, const Session &session, const QString &pamService = QString());
 
     void startSocketServerAndGreeter();
     bool handleAutologinFailure();
 
     bool m_started{false};
+    bool m_authCancellationPending{false};
+    QPointer<QLocalSocket> m_cancelSocket;
 
     int m_terminalId = -1;
     int m_sessionTerminalId = 0;
diff --git a/src/daemon/SocketServer.cpp b/src/daemon/SocketServer.cpp
index 7ac0fe51bce74b9bae8bff436354f7ecea8c0af2..7f971e5a48bb269d73321948262ab334226eee32 100644
--- a/src/daemon/SocketServer.cpp
+++ b/src/daemon/SocketServer.cpp
@@ -133,17 +133,27 @@ void SocketServer::readyRead()
             emit connected();
         } break;
         case GreeterMessages::Login: {
-            // log message
             qDebug() << "Message received from greeter: Login";
 
-            // read username, pasword etc.
-            QString user, password, filename;
+            QString user, password;
             Session session;
             input >> user >> password >> session;
 
-            // emit signal
             emit login(socket, user, password, session);
         } break;
+        case GreeterMessages::FingerprintLogin: {
+            qDebug() << "Message received from greeter: FingerprintLogin";
+
+            QString user;
+            Session session;
+            input >> user >> session;
+
+            emit fingerprintLogin(socket, user, session);
+        } break;
+        case GreeterMessages::CancelLogin: {
+            qDebug() << "Message received from greeter: CancelLogin";
+            emit cancelLogin(socket);
+        } break;
         default: {
             // log message
             qWarning() << "Unknown message" << message;
@@ -162,6 +172,11 @@ void SocketServer::loginSucceeded(QLocalSocket *socket)
     SocketWriter(socket) << quint32(DaemonMessages::LoginSucceeded);
 }
 
+void SocketServer::loginCancelled(QLocalSocket *socket)
+{
+    SocketWriter(socket) << quint32(DaemonMessages::LoginCancelled);
+}
+
 void SocketServer::informationMessage(QLocalSocket *socket, const QString &message)
 {
     SocketWriter(socket) << quint32(DaemonMessages::InformationMessage) << message;
diff --git a/src/daemon/SocketServer.h b/src/daemon/SocketServer.h
index baba7b5627ad12b90035e87342bc71efb06c521e..738f0c8dec568785824f893e0a4e28cd289d4ed1 100644
--- a/src/daemon/SocketServer.h
+++ b/src/daemon/SocketServer.h
@@ -48,9 +48,12 @@ public slots:
     void informationMessage(QLocalSocket *socket, const QString &message);
     void loginFailed(QLocalSocket *socket);
     void loginSucceeded(QLocalSocket *socket);
+    void loginCancelled(QLocalSocket *socket);
 
 signals:
     void login(QLocalSocket *socket, const QString &user, const QString &password, const Session &session);
+    void fingerprintLogin(QLocalSocket *socket, const QString &user, const Session &session);
+    void cancelLogin(QLocalSocket *socket);
     void connected();
 
 private:
diff --git a/src/frontend/greeter/backend/GreeterProxy.cpp b/src/frontend/greeter/backend/GreeterProxy.cpp
index 9e824c340e394d5fcfdda214d4f374550b83f107..16afc4dc581b7ac65c328c688aaf39aa9be18bc5 100644
--- a/src/frontend/greeter/backend/GreeterProxy.cpp
+++ b/src/frontend/greeter/backend/GreeterProxy.cpp
@@ -61,6 +61,16 @@ void GreeterProxy::login(const QString &user, const QString &password, const PLA
     SocketWriter(d->socket) << quint32(GreeterMessages::Login) << user << password << static_cast<uint32_t>(sessionType) << sessionFileName;
 }
 
+void GreeterProxy::fingerprintLogin(const QString &user, const PLASMALOGIN::SessionType sessionType, const QString &sessionFileName) const
+{
+    SocketWriter(d->socket) << quint32(GreeterMessages::FingerprintLogin) << user << static_cast<uint32_t>(sessionType) << sessionFileName;
+}
+
+void GreeterProxy::cancelLogin() const
+{
+    SocketWriter(d->socket) << quint32(GreeterMessages::CancelLogin);
+}
+
 void GreeterProxy::connected()
 {
     // log connection
@@ -115,6 +125,10 @@ void GreeterProxy::readyRead()
             qDebug() << "Information Message received from daemon: " << message;
             emit informationMessage(message);
         } break;
+        case DaemonMessages::LoginCancelled: {
+            qDebug() << "Message received from daemon: LoginCancelled";
+            emit loginCancelled();
+        } break;
         default: {
             // log message
             qWarning() << "Unknown message received from daemon.";
diff --git a/src/frontend/greeter/backend/GreeterProxy.h b/src/frontend/greeter/backend/GreeterProxy.h
index e6c78512882cffd94b3b9a71fd891bf3a818cf4e..19fdc584700a2f9bcd8eefb514940648e5bb06c0 100644
--- a/src/frontend/greeter/backend/GreeterProxy.h
+++ b/src/frontend/greeter/backend/GreeterProxy.h
@@ -41,6 +41,8 @@ public:
 
 public slots:
     void login(const QString &user, const QString &password, const PLASMALOGIN::SessionType sessionType, const QString &sessionFileName) const;
+    void fingerprintLogin(const QString &user, const PLASMALOGIN::SessionType sessionType, const QString &sessionFileName) const;
+    void cancelLogin() const;
 
 private slots:
     void connected();
@@ -54,6 +56,7 @@ signals:
     void socketDisconnected();
     void loginFailed();
     void loginSucceeded();
+    void loginCancelled();
 
 private:
     GreeterProxyPrivate *d{nullptr};
diff --git a/src/frontend/greeter/backend/Messages.h b/src/frontend/greeter/backend/Messages.h
index d4d0be11a1b12961272aec4cbdadd2df4631afae..b5f008009520cf9134e6d46648c0441effc908cc 100644
--- a/src/frontend/greeter/backend/Messages.h
+++ b/src/frontend/greeter/backend/Messages.h
@@ -24,12 +24,15 @@ namespace PLASMALOGIN
 enum class GreeterMessages {
     Connect = 0,
     Login,
+    FingerprintLogin,
+    CancelLogin,
 };
 
 enum class DaemonMessages {
     LoginSucceeded,
     LoginFailed,
     InformationMessage,
+    LoginCancelled,
 };
 
 enum class SessionType {
diff --git a/src/frontend/greeter/mockbackend/MockGreeterProxy.cpp b/src/frontend/greeter/mockbackend/MockGreeterProxy.cpp
index f5ea2abd2d4fc11402222375b1824c2167c61fc4..471109adf4956477f9a3037bfdded1ad1756b46c 100644
--- a/src/frontend/greeter/mockbackend/MockGreeterProxy.cpp
+++ b/src/frontend/greeter/mockbackend/MockGreeterProxy.cpp
@@ -42,4 +42,17 @@ void MockGreeterProxy::login(const QString &user, const QString &password, const
     }
 }
 
+void MockGreeterProxy::fingerprintLogin(const QString &user, const PLASMALOGIN::SessionType sessionType, const QString &sessionFileName) const
+{
+    Q_UNUSED(user)
+    Q_UNUSED(sessionType)
+    Q_UNUSED(sessionFileName)
+    QTimer::singleShot(250, this, &MockGreeterProxy::loginFailed);
+}
+
+void MockGreeterProxy::cancelLogin() const
+{
+    QTimer::singleShot(0, this, &MockGreeterProxy::loginCancelled);
+}
+
 #include "moc_MockGreeterProxy.cpp"
diff --git a/src/frontend/greeter/mockbackend/MockGreeterProxy.h b/src/frontend/greeter/mockbackend/MockGreeterProxy.h
index 7adef8f82085d15bcc64074a38b5d59eb27758ca..d26e09c8eabd102cd76d5e3e06742a2e014ff6a7 100644
--- a/src/frontend/greeter/mockbackend/MockGreeterProxy.h
+++ b/src/frontend/greeter/mockbackend/MockGreeterProxy.h
@@ -16,6 +16,8 @@ public:
     MockGreeterProxy();
 public Q_SLOTS:
     void login(const QString &user, const QString &password, const PLASMALOGIN::SessionType sessionType, const QString &sessionFileName) const;
+    void fingerprintLogin(const QString &user, const PLASMALOGIN::SessionType sessionType, const QString &sessionFileName) const;
+    void cancelLogin() const;
 
 Q_SIGNALS:
     void informationMessage(const QString &message);
@@ -23,6 +25,7 @@ Q_SIGNALS:
     void socketDisconnected();
     void loginFailed();
     void loginSucceeded();
+    void loginCancelled();
 
 private:
     static constexpr QLatin1String s_mockPassword = QLatin1String("mypassword");
diff --git a/src/frontend/greeter/qml/GreeterState.qml b/src/frontend/greeter/qml/GreeterState.qml
index 68395e945d4def1573620f61e1b90f9390d3ac4e..b016da7338b1dfada624cd892838883f4135849c 100644
--- a/src/frontend/greeter/qml/GreeterState.qml
+++ b/src/frontend/greeter/qml/GreeterState.qml
@@ -147,6 +147,13 @@ Item {
         PlasmaLogin.Authenticator.login(username, password, sessionType, sessionFileName);
     }
 
+    function handleFingerprintLoginRequest(username, sessionType, sessionFileName) {
+        greeterState.lastLoggedInUser = username;
+        greeterState.lastLoggedInSession = sessionFileName;
+
+        PlasmaLogin.Authenticator.fingerprintLogin(username, sessionType, sessionFileName);
+    }
+
     Connections {
         target: PlasmaLogin.Authenticator
 
diff --git a/src/frontend/greeter/qml/Login.qml b/src/frontend/greeter/qml/Login.qml
index 9437711e55bc0e4746a4dc10fecc3b5957fbac65..c7e798570ee423721bcf23240b5ea206c116ab8b 100644
--- a/src/frontend/greeter/qml/Login.qml
+++ b/src/frontend/greeter/qml/Login.qml
@@ -30,6 +30,8 @@ SessionManagementScreen {
     // after a timeout: one automatic attempt per selected user/greeter.
     property bool fingerprintAutoAttemptDone: false
     property bool fingerprintAutoAttemptInFlight: false
+    property bool fingerprintCancelInFlight: false
+    property bool pendingPasswordLogin: false
 
     //the y position that should be ensured visible when the on screen keyboard is visible
     property int visibleBoundary: mapFromItem(loginButton, 0, 0).y
@@ -38,6 +40,7 @@ SessionManagementScreen {
     property real fontSize: Kirigami.Theme.defaultFont.pointSize
 
     signal loginRequest(string username, string password)
+    signal fingerprintLoginRequest(string username)
 
     function maybeStartFingerprintLogin() {
         if (fingerprintAutoAttemptDone
@@ -57,7 +60,7 @@ SessionManagementScreen {
                 || passwordBox.text.length !== 0) {
                 return;
             }
-            startLogin(true);
+            startFingerprintLogin();
         });
     }
 
@@ -94,21 +97,46 @@ SessionManagementScreen {
      * Login has been requested with the following username and password
      * If username field is visible, it will be taken from that, otherwise from the "name" property of the currentIndex
      */
-    function startLogin(automaticFingerprint) {
+    function startFingerprintLogin() {
+        if (fingerprintAutoAttemptInFlight || fingerprintCancelInFlight) {
+            return;
+        }
+
+        const username = userList.selectedUser
+        if (!username || passwordBox.text.length !== 0) {
+            return;
+        }
+
+        fingerprintAutoAttemptInFlight = true
+        fingerprintLoginRequest(username)
+    }
+
+    function cancelFingerprintForPassword(submitAfterCancel) {
+        if (submitAfterCancel) {
+            pendingPasswordLogin = true
+        }
+
+        if (!fingerprintAutoAttemptInFlight || fingerprintCancelInFlight) {
+            return;
+        }
+
+        fingerprintCancelInFlight = true
+        PlasmaLogin.Authenticator.cancelLogin()
+    }
+
+    function startLogin() {
         const username = showUsernamePrompt ? userNameInput.text : userList.selectedUser
         const password = passwordBox.text
 
-        fingerprintAutoAttemptInFlight = automaticFingerprint === true
+        if (fingerprintAutoAttemptInFlight || fingerprintCancelInFlight) {
+            cancelFingerprintForPassword(true)
+            return
+        }
 
         footer.enabled = false
         mainStack.enabled = false
         userListComponent.userList.opacity = 0.75
 
-        // This is partly because it looks nicer, but more importantly it
-        // works round a Qt bug that can trigger if the app is closed with a
-        // TextField focused.
-        //
-        // See https://bugreports.qt.io/browse/QTBUG-55460
         loginButton.forceActiveFocus();
         loginRequest(username, password);
     }
@@ -141,6 +169,12 @@ SessionManagementScreen {
             placeholderText: i18nd("plasma_login", "Password")
             focus: !showUsernamePrompt
 
+            onTextChanged: {
+                if (text.length > 0 && root.fingerprintAutoAttemptInFlight) {
+                    root.cancelFingerprintForPassword(false)
+                }
+            }
+
             onAccepted: {
                 if (root.loginScreenUiVisible) {
                     startLogin();
@@ -173,6 +207,17 @@ SessionManagementScreen {
                     passwordBox.selectAll()
                     passwordBox.forceActiveFocus()
                 }
+
+                function onLoginCancelled() {
+                    root.fingerprintAutoAttemptInFlight = false
+                    root.fingerprintCancelInFlight = false
+                    if (root.pendingPasswordLogin) {
+                        root.pendingPasswordLogin = false
+                        Qt.callLater(root.startLogin)
+                    } else {
+                        passwordBox.forceActiveFocus()
+                    }
+                }
             }
         }
 
@@ -223,6 +268,8 @@ SessionManagementScreen {
 
                 root.fingerprintAutoAttemptDone = false;
                 root.fingerprintAutoAttemptInFlight = false;
+                root.fingerprintCancelInFlight = false;
+                root.pendingPasswordLogin = false;
 
                 if (PlasmaLogin.GreeterState.userListIndex != root.userList.currentIndex) {
                     PlasmaLogin.GreeterState.userListIndex = root.userList.currentIndex;
diff --git a/src/frontend/greeter/qml/Main.qml b/src/frontend/greeter/qml/Main.qml
index b53dfe32c1888b916d6642eb35674f8e1ebdc6bf..7a7e72500d172d694605528ec641938bb32722b1 100644
--- a/src/frontend/greeter/qml/Main.qml
+++ b/src/frontend/greeter/qml/Main.qml
@@ -212,6 +212,7 @@ Item {
                     }]
 
                 onLoginRequest: (username, password) => root.handleLoginRequest(username, password, sessionButton.currentSessionType, sessionButton.currentSessionFileName)
+                onFingerprintLoginRequest: username => root.handleFingerprintLoginRequest(username, sessionButton.currentSessionType, sessionButton.currentSessionFileName)
             }
 
             readonly property real zoomFactor: 1.5
@@ -310,6 +311,7 @@ Item {
                 }
 
                 onLoginRequest: (username, password) => root.handleLoginRequest(username, password, sessionButton.currentSessionType, sessionButton.currentSessionFileName)
+                onFingerprintLoginRequest: username => root.handleFingerprintLoginRequest(username, sessionButton.currentSessionType, sessionButton.currentSessionFileName)
 
                 //actionItemsVisible: !inputPanel.keyboardActive
                 actionItems: [
@@ -414,6 +416,11 @@ Item {
         PlasmaLogin.GreeterState.handleLoginRequest(username, password, sessionType, sessionFileName);
     }
 
+    function handleFingerprintLoginRequest(username, sessionType, sessionFileName) {
+        root.notificationMessage = "";
+        PlasmaLogin.GreeterState.handleFingerprintLoginRequest(username, sessionType, sessionFileName);
+    }
+
     Connections {
         target: PlasmaLogin.Authenticator
 
@@ -432,6 +439,8 @@ Item {
             // spurious generic login failure.
             if (userListComponent.fingerprintAutoAttemptInFlight) {
                 userListComponent.fingerprintAutoAttemptInFlight = false;
+                userListComponent.fingerprintCancelInFlight = false;
+                userListComponent.pendingPasswordLogin = false;
                 notificationMessage = "";
                 notificationResetTimer.stop();
 
diff --git a/src/helper/HelperApp.cpp b/src/helper/HelperApp.cpp
index 61553d39e07cec8a598825fc4c688e04d37d3a90..108e5caadcb28b9101bf2e147028baeaaaf8158a 100644
--- a/src/helper/HelperApp.cpp
+++ b/src/helper/HelperApp.cpp
@@ -103,6 +103,15 @@ void HelperApp::setUp()
             return;
         }
     }
+    if ((pos = args.indexOf(QStringLiteral("--pam-service"))) >= 0) {
+        if (pos >= args.length() - 1) {
+            qCritical() << "Missing PAM service";
+            exit(Auth::HELPER_OTHER_ERROR);
+            return;
+        }
+        m_backend->setPamService(args[pos + 1]);
+    }
+
     if ((pos = args.indexOf(QStringLiteral("--autologin"))) >= 0) {
         m_backend->setAutologin(true);
     }
diff --git a/src/helper/backend/PamBackend.cpp b/src/helper/backend/PamBackend.cpp
index 50f386d77dffecb89dfa8889cb234219aa21e2e7..ff0ffda32224a50a89611924fd0b350c9c86e1c0 100644
--- a/src/helper/backend/PamBackend.cpp
+++ b/src/helper/backend/PamBackend.cpp
@@ -213,7 +213,7 @@ bool PamBackend::start(const QString &user)
 {
     bool result;
 
-    QString service = QStringLiteral("plasmalogin");
+    QString service = m_pamService.isEmpty() ? QStringLiteral("plasmalogin") : m_pamService;
 
     if (user == QStringLiteral("plasmalogin") && m_greeter) {
         service = QStringLiteral("plasmalogin-greeter");
@@ -391,6 +391,17 @@ void PamBackend::setGreeter(bool on)
 {
     m_greeter = on;
 }
+
+void PamBackend::setPamService(const QString &service)
+{
+    // The helper runs as root; never permit the greeter to select an arbitrary
+    // PAM stack. Only our dedicated fingerprint-only service is accepted.
+    if (service == QStringLiteral("plasmalogin-fingerprint")) {
+        m_pamService = service;
+    } else {
+        m_pamService.clear();
+    }
+}
 }
 
 #include "moc_PamBackend.cpp"
diff --git a/src/helper/backend/PamBackend.h b/src/helper/backend/PamBackend.h
index e150e76fa5b95fd9f01e31c9e8a7a964cd3343be..2b962b87673ccd8060f927467f18c2134075a743 100644
--- a/src/helper/backend/PamBackend.h
+++ b/src/helper/backend/PamBackend.h
@@ -53,6 +53,7 @@ public:
     int converse(int n, const struct pam_message **msg, struct pam_response **resp);
     void setAutologin(bool on = true);
     void setGreeter(bool on = true);
+    void setPamService(const QString &service);
 
 public slots:
     bool start(const QString &user = QString());
@@ -66,6 +67,7 @@ private:
     HelperApp *m_app{nullptr};
     bool m_autologin{false};
     bool m_greeter{false};
+    QString m_pamService{};
     PamData *m_data{nullptr};
     PamHandle *m_pam{nullptr};
 };
