diff --git a/src/daemon/Display.cpp b/src/daemon/Display.cpp
index 143e3e4..62cc698 100644
--- a/src/daemon/Display.cpp
+++ b/src/daemon/Display.cpp
@@ -89,6 +89,7 @@ int fetchAvailableVt()
 Display::Display(Seat *parent)
     : QObject(parent)
     , m_auth(new Auth(this))
+    , m_fingerprintAuth(new Auth(this))
     , m_seat(parent)
     , m_socketServer(new SocketServer(this))
     , m_greeter(new Greeter(this))
@@ -107,6 +108,15 @@ Display::Display(Seat *parent)
     connect(m_auth, &Auth::info, this, &Display::slotAuthInfo);
     connect(m_auth, &Auth::error, this, &Display::slotAuthError);

+    // Separate fingerprint worker: password and fingerprint stay active concurrently.
+    m_fingerprintAuth->setVerbose(true);
+    connect(m_fingerprintAuth, &Auth::requestChanged, this, &Display::slotFingerprintRequestChanged);
+    connect(m_fingerprintAuth, &Auth::authentication, this, &Display::slotFingerprintAuthenticationFinished);
+    connect(m_fingerprintAuth, &Auth::sessionStarted, this, &Display::slotSessionStarted);
+    connect(m_fingerprintAuth, &Auth::finished, this, &Display::slotFingerprintHelperFinished);
+    connect(m_fingerprintAuth, &Auth::info, this, &Display::slotFingerprintAuthInfo);
+    connect(m_fingerprintAuth, &Auth::error, this, &Display::slotFingerprintAuthError);
+
     // connect authentication requests from the greeter.
     connect(m_socketServer, &SocketServer::login, this, &Display::login);
     connect(m_socketServer, &SocketServer::fingerprintLogin, this, &Display::fingerprintLogin);
@@ -146,12 +156,13 @@ Display::Display(Seat *parent)
 Display::~Display()
 {
     disconnect(m_auth, &Auth::finished, this, &Display::slotHelperFinished);
+    disconnect(m_fingerprintAuth, &Auth::finished, this, &Display::slotFingerprintHelperFinished);
     stop();
 }

 int Display::terminalId() const
 {
-    return m_auth->isActive() ? m_sessionTerminalId : m_terminalId;
+    return (m_auth->isActive() || m_fingerprintAuth->isActive()) ? m_sessionTerminalId : m_terminalId;
 }

 QString Display::sessionType() const
@@ -176,7 +187,7 @@ bool Display::start()
     // (rootful X + X11 autologin session).
     if (m_autologinSession.isValid()) {
         m_auth->setAutologin(true);
-        if (startAuth(PlasmaLogin::config()->autologinUser(), QString(), m_autologinSession)) {
+        if (startAuth(m_auth, PlasmaLogin::config()->autologinUser(), QString(), m_autologinSession)) {
             return true;
         } else {
             return handleAutologinFailure();
@@ -235,6 +246,7 @@ void Display::stop()
     // stop the greeter
     m_greeter->stop();

+    m_fingerprintAuth->stop();
     m_auth->stop();

     // stop socket server
@@ -251,71 +263,40 @@ void Display::login(QLocalSocket *socket, const QString &user, const QString &pa
 {
     m_socket = socket;

-    // the PLASMALOGIN user has special privileges that skip password checking so that we can load the greeter
-    // block ever trying to log in as the PLASMALOGIN user
     if (user == QLatin1String("plasmalogin")) {
         emit loginFailed(m_socket);
         return;
     }

-    // Password must always win over the background fingerprint helper.  The
-    // greeter normally sends CancelLogin first, but daemon-side preemption is
-    // required as a safety net because intermediate pam_fprintd errors can race
-    // UI state. Never discard an already submitted password.
-    if (m_auth->isActive() &&
-        m_auth->pamService() == QStringLiteral("plasmalogin-fingerprint")) {
-        qDebug() << "Password login preempts active fingerprint authentication";
-        m_passwordPreemptionPending = true;
-        m_pendingPasswordSocket = socket;
-        m_pendingPasswordUser = user;
-        m_pendingPassword = password;
-        m_pendingPasswordSession = session;
-        m_authCancellationPending = false;
-        m_cancelSocket = nullptr;
-        m_auth->stop();
-        return;
-    }
-
-    // Password authentication always uses the normal plasmalogin PAM stack.
-    startAuth(user, password, session);
+    // Password authentication is independent from fingerprint authentication.
+    startAuth(m_auth, user, password, session);
 }

 void Display::fingerprintLogin(QLocalSocket *socket, const QString &user, const Session &session)
 {
-    m_socket = socket;
-    startAuth(user, QString(), session, QStringLiteral("plasmalogin-fingerprint"));
+    m_fingerprintSocket = socket;
+    startAuth(m_fingerprintAuth, user, QString(), session, QStringLiteral("plasmalogin-fingerprint"));
 }

 void Display::cancelLogin(QLocalSocket *socket)
 {
-    if (socket != m_socket || !m_auth->isActive()) {
+    if (socket != m_fingerprintSocket || !m_fingerprintAuth->isActive()) {
         m_socketServer->loginCancelled(socket);
         return;
     }

     m_authCancellationPending = true;
     m_cancelSocket = socket;
-    m_socket = nullptr;
-    m_auth->stop();
+    m_fingerprintSocket = nullptr;
+    m_fingerprintAuth->stop();
 }

-bool Display::startAuth(const QString &user, const QString &password, const Session &session, const QString &pamService)
+bool Display::prepareAuthContext(const QString &user, const Session &session)
 {
-    qDebug() << "start auth" << "user" << session.isValid() << session.exec();
-
-    if (m_auth->isActive()) {
-        qWarning() << "Existing authentication ongoing, aborting";
-        return false;
-    }
-
-    m_passPhrase = password;
-
-    // sanity check
     if (!session.isValid()) {
         qCritical() << "Invalid session" << session.fileName();
         return false;
     }
-
     if (session.xdgSessionType().isEmpty()) {
         qCritical() << "Failed to find XDG session type for session" << session.fileName();
         return false;
@@ -325,18 +306,30 @@ bool Display::startAuth(const QString &user, const QString &password, const Sess
         return false;
     }

+    const bool sameContext = m_authContextPrepared
+        && m_authContextUser == user
+        && m_authContextSessionFile == session.fileName();
+    if (sameContext) {
+        return true;
+    }
+
+    if (m_auth->isActive() || m_fingerprintAuth->isActive()) {
+        qWarning() << "Refusing to switch user/session while authentication is active";
+        return false;
+    }
+
+    m_loginCommitted = false;
+    m_committedAuth = nullptr;
     m_reuseSessionId = QString();

     if (Logind::isAvailable()) {
         OrgFreedesktopLogin1ManagerInterface manager(Logind::serviceName(), Logind::managerPath(), QDBusConnection::systemBus());
         auto reply = manager.ListSessions();
         reply.waitForFinished();
-
-        const auto info = reply.value();
         for (const SessionInfo &s : reply.value()) {
             if (s.userName == user) {
-                OrgFreedesktopLogin1SessionInterface session(Logind::serviceName(), s.sessionPath.path(), QDBusConnection::systemBus());
-                if (session.service() == QLatin1String("plasmalogin") && session.state() == QLatin1String("online")) {
+                OrgFreedesktopLogin1SessionInterface candidate(Logind::serviceName(), s.sessionPath.path(), QDBusConnection::systemBus());
+                if (candidate.service() == QLatin1String("plasmalogin") && candidate.state() == QLatin1String("online")) {
                     m_reuseSessionId = s.sessionId;
                     break;
                 }
@@ -344,22 +337,12 @@ bool Display::startAuth(const QString &user, const QString &password, const Sess
         }
     }

-    // save session desktop file name, we'll use it to set the
-    // last session later, in slotAuthenticationFinished()
     m_sessionName = session.fileName();
-
     m_sessionTerminalId = m_terminalId;
-
-    if (m_greeter->isRunning()) {
-        // Create a new VT when we need to have another compositor running
-        if (seat()->canTTY()) {
-            m_sessionTerminalId = VirtualTerminal::setUpNewVt();
-        }
+    if (m_greeter->isRunning() && seat()->canTTY()) {
+        m_sessionTerminalId = VirtualTerminal::setUpNewVt();
     }

-    // some information
-    qDebug() << "Session" << m_sessionName << "selected, command:" << session.exec() << "for VT" << m_sessionTerminalId << session.xdgSessionType();
-
     QProcessEnvironment env;
     env.insert(QStringLiteral("PATH"), PlasmaLogin::config()->defaultPath());
     env.insert(QStringLiteral("XDG_SEAT_PATH"), daemonApp->displayManager()->seatPath(seat()->name()));
@@ -376,104 +359,179 @@ bool Display::startAuth(const QString &user, const QString &password, const Sess
     }
     env.insert(QStringLiteral("XDG_SESSION_DESKTOP"), session.desktopNames());

-    m_auth->setUser(user);
-    m_auth->setPamService(pamService);
-    if (m_reuseSessionId.isNull()) {
-        m_auth->setSession(session.exec());
+    m_authEnvironment = env;
+    m_authContextUser = user;
+    m_authContextSessionFile = session.fileName();
+    m_authContextPrepared = true;
+    qDebug() << "Prepared shared auth context for" << user << m_sessionName << "VT" << m_sessionTerminalId;
+    return true;
+}
+
+bool Display::startAuth(Auth *auth, const QString &user, const QString &password, const Session &session, const QString &pamService)
+{
+    if (!auth || auth->isActive()) {
+        qWarning() << "Requested authenticator already active" << pamService;
+        return false;
+    }
+    if (m_loginCommitted) {
+        qWarning() << "Login already committed; refusing another authenticator";
+        return false;
+    }
+    if (!prepareAuthContext(user, session)) {
+        return false;
     }
-    m_auth->insertEnvironment(env);
-    m_auth->start();

+    if (auth == m_auth) {
+        m_passPhrase = password;
+    }
+
+    auth->setUser(user);
+    auth->setPamService(pamService);
+    auth->setSession(m_reuseSessionId.isNull() ? session.exec() : QString());
+    auth->insertEnvironment(m_authEnvironment);
+    auth->start();
+    qDebug() << "Started authenticator" << (auth == m_fingerprintAuth ? "fingerprint" : "password") << "for" << user;
     return true;
 }

-void Display::slotAuthenticationFinished(const QString &user, bool success)
+void Display::commitSuccessfulAuth(Auth *winner, QLocalSocket *socket)
+{
+    if (m_loginCommitted) {
+        if (winner != m_committedAuth && winner->isActive()) {
+            winner->stop();
+        }
+        return;
+    }
+
+    m_loginCommitted = true;
+    m_committedAuth = winner;
+    qDebug() << "Authentication race won by" << (winner == m_fingerprintAuth ? "fingerprint" : "password");
+
+    if (winner != m_auth && m_auth->isActive()) {
+        m_auth->stop();
+        m_socket = nullptr;
+    }
+    if (winner != m_fingerprintAuth && m_fingerprintAuth->isActive()) {
+        m_fingerprintAuth->stop();
+        m_fingerprintSocket = nullptr;
+    }
+
+    if (!m_reuseSessionId.isNull()) {
+        OrgFreedesktopLogin1ManagerInterface manager(Logind::serviceName(), Logind::managerPath(), QDBusConnection::systemBus());
+        manager.UnlockSession(m_reuseSessionId);
+        manager.ActivateSession(m_reuseSessionId);
+    }
+
+    if (socket) {
+        emit loginSucceeded(socket);
+    }
+}
+
+void Display::resetAuthContextIfIdle()
 {
-    // stop() may still produce an authentication result while a fingerprint
-    // cancellation/preemption is draining.  It is not a terminal login result
-    // for the greeter; slotHelperFinished owns the transition.
-    if (m_authCancellationPending || m_passwordPreemptionPending) {
-        qDebug() << "Ignoring authentication result while fingerprint stop is pending";
+    if (m_loginCommitted || m_auth->isActive() || m_fingerprintAuth->isActive()) {
         return;
     }

+    m_authContextPrepared = false;
+    m_authContextUser.clear();
+    m_authContextSessionFile.clear();
+    m_authEnvironment = QProcessEnvironment();
+    m_committedAuth = nullptr;
+}
+
+void Display::slotAuthenticationFinished(const QString &user, bool success)
+{
     if (m_auth->autologin() && !success) {
         handleAutologinFailure();
         return;
     }

     if (success) {
-        qDebug() << "Authentication for user " << user << " successful";
-
-        if (!m_reuseSessionId.isNull()) {
-            OrgFreedesktopLogin1ManagerInterface manager(Logind::serviceName(), Logind::managerPath(), QDBusConnection::systemBus());
-            manager.UnlockSession(m_reuseSessionId);
-            manager.ActivateSession(m_reuseSessionId);
-        }
+        commitSuccessfulAuth(m_auth, m_socket);
+        return;
+    }

-        if (m_socket) {
-            emit loginSucceeded(m_socket);
-        }
-    } else if (m_socket) {
-        qDebug() << "Authentication for user " << user << " failed";
+    if (!m_loginCommitted && m_socket) {
+        qDebug() << "Password authentication for user" << user << "failed";
         emit loginFailed(m_socket);
     }
     m_socket = nullptr;
+    resetAuthContextIfIdle();
 }

-void Display::slotAuthInfo(const QString &message, Auth::Info info)
+void Display::slotFingerprintAuthenticationFinished(const QString &user, bool success)
 {
-    qWarning() << "Authentication information:" << info << message;
-
-    if (!m_socket) {
+    if (success) {
+        commitSuccessfulAuth(m_fingerprintAuth, m_fingerprintSocket);
         return;
     }

-    m_socketServer->informationMessage(m_socket, message);
+    qDebug() << "Fingerprint authentication for user" << user << "did not match";
+}
+
+void Display::slotAuthInfo(const QString &message, Auth::Info info)
+{
+    qWarning() << "Password authentication information:" << info << message;
+    if (m_socket) {
+        m_socketServer->informationMessage(m_socket, message);
+    }
+}
+
+void Display::slotFingerprintAuthInfo(const QString &message, Auth::Info info)
+{
+    qWarning() << "Fingerprint authentication information:" << info << message;
+    if (m_fingerprintSocket) {
+        m_socketServer->informationMessage(m_fingerprintSocket, message);
+    }
 }

 void Display::slotAuthError(const QString &message, Auth::Error error)
 {
-    qWarning() << "Authentication error:" << error << message;
+    qWarning() << "Password authentication error:" << error << message;
+    if (m_socket) {
+        m_socketServer->informationMessage(m_socket, message);
+    }
+}
+
+void Display::slotFingerprintAuthError(const QString &message, Auth::Error error)
+{
+    qWarning() << "Fingerprint authentication error:" << error << message;
+    if (m_fingerprintSocket) {
+        m_socketServer->informationMessage(m_fingerprintSocket, message);
+    }
+}

-    if (!m_socket) {
+void Display::slotHelperFinished(Auth::HelperExitStatus status)
+{
+    if (m_loginCommitted && m_committedAuth != m_auth) {
         return;
     }

-    m_socketServer->informationMessage(m_socket, message);
+    if (m_loginCommitted && m_committedAuth == m_auth) {
+        if (status != Auth::HELPER_AUTH_ERROR) {
+            stop();
+        }
+        return;
+    }

-    // pam_fprintd can emit ERROR_AUTHENTICATION for an individual rejected
-    // finger pose while its helper remains active and is about to request the
-    // next pose.  Treating that message as terminal desynchronises the greeter:
-    // it clears its fingerprint state although the PAM helper still owns Auth.
-    // The real terminal result is delivered by slotAuthenticationFinished().
-    if (error == Auth::ERROR_AUTHENTICATION &&
-        m_auth->pamService() != QStringLiteral("plasmalogin-fingerprint")) {
+    if (status != Auth::HELPER_AUTH_ERROR && m_socket) {
+        qWarning() << "Password helper ended without authentication; keeping other authenticators alive";
         emit loginFailed(m_socket);
+        m_socket = nullptr;
     }
+
+    resetAuthContextIfIdle();
 }

-void Display::slotHelperFinished(Auth::HelperExitStatus status)
+void Display::slotFingerprintHelperFinished(Auth::HelperExitStatus status)
 {
-    if (m_passwordPreemptionPending) {
-        const QPointer<QLocalSocket> socket = m_pendingPasswordSocket;
-        const QString user = m_pendingPasswordUser;
-        const QString password = m_pendingPassword;
-        const Session session = m_pendingPasswordSession;
-
-        m_passwordPreemptionPending = false;
-        m_pendingPasswordSocket = nullptr;
-        m_pendingPasswordUser.clear();
-        m_pendingPassword.clear();
-        m_authCancellationPending = false;
-        m_cancelSocket = nullptr;
-        m_socket = socket;
-
-        qDebug() << "Fingerprint helper stopped; starting queued password authentication";
-        if (!socket || !startAuth(user, password, session)) {
-            if (socket) {
-                emit loginFailed(socket);
-            }
+    if (m_loginCommitted) {
+        if (m_committedAuth != m_fingerprintAuth) {
+            return;
+        }
+        if (status != Auth::HELPER_AUTH_ERROR) {
+            stop();
         }
         return;
     }
@@ -484,17 +542,14 @@ void Display::slotHelperFinished(Auth::HelperExitStatus status)
             m_socketServer->loginCancelled(m_cancelSocket);
             m_cancelSocket = nullptr;
         }
-        return;
+    } else if (m_fingerprintSocket) {
+        // Terminal fingerprint failure only ends the fingerprint worker.
+        // Password authentication remains available and independent.
+        m_socketServer->loginCancelled(m_fingerprintSocket);
     }

-    // Don't restart greeter and display server unless plasmalogin-helper exited
-    // with an internal error or the user session finished successfully,
-    // we want to avoid greeter from restarting when an authentication
-    // error happens (in this case we want to show the message from the
-    // greeter
-    if (status != Auth::HELPER_AUTH_ERROR) {
-        stop();
-    }
+    m_fingerprintSocket = nullptr;
+    resetAuthContextIfIdle();
 }

 void Display::slotRequestChanged()
@@ -509,8 +564,26 @@ void Display::slotRequestChanged()
     }
 }

+void Display::slotFingerprintRequestChanged()
+{
+    if (m_fingerprintAuth->request()->prompts().length() == 1) {
+        m_fingerprintAuth->request()->prompts()[0]->setResponse("");
+        m_fingerprintAuth->request()->done();
+    } else if (m_fingerprintAuth->request()->prompts().length() == 2) {
+        m_fingerprintAuth->request()->prompts()[0]->setResponse(qPrintable(m_fingerprintAuth->user()));
+        m_fingerprintAuth->request()->prompts()[1]->setResponse("");
+        m_fingerprintAuth->request()->done();
+    }
+}
+
 void Display::slotSessionStarted(bool success)
 {
+    Auth *source = qobject_cast<Auth *>(sender());
+    if (m_loginCommitted && source && source != m_committedAuth) {
+        qWarning() << "Ignoring session result from losing authenticator";
+        return;
+    }
+
     qDebug() << "Session started" << success;
     if (success) {
         QTimer::singleShot(5000, m_greeter, &Greeter::stop);
diff --git a/src/daemon/Display.h b/src/daemon/Display.h
index da977b5..a57249c 100644
--- a/src/daemon/Display.h
+++ b/src/daemon/Display.h
@@ -22,6 +22,7 @@
 #include <QDir>
 #include <QObject>
 #include <QPointer>
+#include <QProcessEnvironment>

 #include "Auth.h"
 #include "Session.h"
@@ -71,7 +72,10 @@ signals:
     void loginSucceeded(QLocalSocket *socket);

 private:
-    bool startAuth(const QString &user, const QString &password, const Session &session, const QString &pamService = QString());
+    bool prepareAuthContext(const QString &user, const Session &session);
+    bool startAuth(Auth *auth, const QString &user, const QString &password, const Session &session, const QString &pamService = QString());
+    void commitSuccessfulAuth(Auth *winner, QLocalSocket *socket);
+    void resetAuthContextIfIdle();

     void startSocketServerAndGreeter();
     bool handleAutologinFailure();
@@ -80,14 +84,12 @@ private:
     bool m_authCancellationPending{false};
     QPointer<QLocalSocket> m_cancelSocket;

-    // Server-side safety net: if a password Login arrives while the dedicated
-    // fingerprint PAM helper is still alive, keep the submitted credentials,
-    // stop fingerprint auth, and launch password PAM as soon as the helper exits.
-    bool m_passwordPreemptionPending{false};
-    QPointer<QLocalSocket> m_pendingPasswordSocket;
-    QString m_pendingPasswordUser;
-    QString m_pendingPassword;
-    Session m_pendingPasswordSession;
+    bool m_loginCommitted{false};
+    bool m_authContextPrepared{false};
+    QString m_authContextUser;
+    QString m_authContextSessionFile;
+    QProcessEnvironment m_authEnvironment;
+    Auth *m_committedAuth{nullptr};

     int m_terminalId = -1;
     int m_sessionTerminalId = 0;
@@ -99,18 +101,25 @@ private:
     Session m_autologinSession;

     Auth *m_auth{nullptr};
+    Auth *m_fingerprintAuth{nullptr};
     Seat *m_seat{nullptr};
     SocketServer *m_socketServer{nullptr};
     QPointer<QLocalSocket> m_socket;
+    QPointer<QLocalSocket> m_fingerprintSocket;
     Greeter *m_greeter{nullptr};

 private slots:
     void slotRequestChanged();
+    void slotFingerprintRequestChanged();
     void slotAuthenticationFinished(const QString &user, bool success);
+    void slotFingerprintAuthenticationFinished(const QString &user, bool success);
     void slotSessionStarted(bool success);
     void slotHelperFinished(Auth::HelperExitStatus status);
+    void slotFingerprintHelperFinished(Auth::HelperExitStatus status);
     void slotAuthInfo(const QString &message, Auth::Info info);
+    void slotFingerprintAuthInfo(const QString &message, Auth::Info info);
     void slotAuthError(const QString &message, Auth::Error error);
+    void slotFingerprintAuthError(const QString &message, Auth::Error error);
 };
 }

diff --git a/src/frontend/greeter/qml/Login.qml b/src/frontend/greeter/qml/Login.qml
index c7e7985..c968ba2 100644
--- a/src/frontend/greeter/qml/Login.qml
+++ b/src/frontend/greeter/qml/Login.qml
@@ -31,7 +31,7 @@ SessionManagementScreen {
     property bool fingerprintAutoAttemptDone: false
     property bool fingerprintAutoAttemptInFlight: false
     property bool fingerprintCancelInFlight: false
-    property bool pendingPasswordLogin: false
+    property bool pendingFingerprintRestart: false

     //the y position that should be ensured visible when the on screen keyboard is visible
     property int visibleBoundary: mapFromItem(loginButton, 0, 0).y
@@ -46,8 +46,7 @@ SessionManagementScreen {
         if (fingerprintAutoAttemptDone
             || !loginScreenUiVisible
             || showUsernamePrompt
-            || !userList.selectedUser
-            || passwordBox.text.length !== 0) {
+            || !userList.selectedUser) {
             return;
         }

@@ -56,8 +55,7 @@ SessionManagementScreen {
             // The user may have started typing before the queued call runs.
             if (!loginScreenUiVisible
                 || showUsernamePrompt
-                || !userList.selectedUser
-                || passwordBox.text.length !== 0) {
+                || !userList.selectedUser) {
                 return;
             }
             startFingerprintLogin();
@@ -103,7 +101,7 @@ SessionManagementScreen {
         }

         const username = userList.selectedUser
-        if (!username || passwordBox.text.length !== 0) {
+        if (!username) {
             return;
         }

@@ -111,16 +109,13 @@ SessionManagementScreen {
         fingerprintLoginRequest(username)
     }

-    function cancelFingerprintForPassword(submitAfterCancel) {
-        if (submitAfterCancel) {
-            pendingPasswordLogin = true
-        }
-
+    function cancelFingerprintForUserChange()
+    {
         if (!fingerprintAutoAttemptInFlight || fingerprintCancelInFlight) {
-            return;
+            return
         }
-
         fingerprintCancelInFlight = true
+        pendingFingerprintRestart = true
         PlasmaLogin.Authenticator.cancelLogin()
     }

@@ -128,11 +123,8 @@ SessionManagementScreen {
         const username = showUsernamePrompt ? userNameInput.text : userList.selectedUser
         const password = passwordBox.text

-        if (fingerprintAutoAttemptInFlight || fingerprintCancelInFlight) {
-            cancelFingerprintForPassword(true)
-            return
-        }
-
+        // Password and fingerprint are intentionally independent. Typing or
+        // submitting a password must not cancel the active fingerprint worker.
         footer.enabled = false
         mainStack.enabled = false
         userListComponent.userList.opacity = 0.75
@@ -169,12 +161,6 @@ SessionManagementScreen {
             placeholderText: i18nd("plasma_login", "Password")
             focus: !showUsernamePrompt

-            onTextChanged: {
-                if (text.length > 0 && root.fingerprintAutoAttemptInFlight) {
-                    root.cancelFingerprintForPassword(false)
-                }
-            }
-
             onAccepted: {
                 if (root.loginScreenUiVisible) {
                     startLogin();
@@ -211,9 +197,10 @@ SessionManagementScreen {
                 function onLoginCancelled() {
                     root.fingerprintAutoAttemptInFlight = false
                     root.fingerprintCancelInFlight = false
-                    if (root.pendingPasswordLogin) {
-                        root.pendingPasswordLogin = false
-                        Qt.callLater(root.startLogin)
+                    if (root.pendingFingerprintRestart) {
+                        root.pendingFingerprintRestart = false
+                        root.fingerprintAutoAttemptDone = false
+                        Qt.callLater(root.maybeStartFingerprintLogin)
                     } else {
                         passwordBox.forceActiveFocus()
                     }
@@ -267,9 +254,12 @@ SessionManagementScreen {
                 }

                 root.fingerprintAutoAttemptDone = false;
-                root.fingerprintAutoAttemptInFlight = false;
-                root.fingerprintCancelInFlight = false;
-                root.pendingPasswordLogin = false;
+                if (root.fingerprintAutoAttemptInFlight) {
+                    root.cancelFingerprintForUserChange();
+                } else {
+                    root.fingerprintCancelInFlight = false;
+                    root.pendingFingerprintRestart = false;
+                }

                 if (PlasmaLogin.GreeterState.userListIndex != root.userList.currentIndex) {
                     PlasmaLogin.GreeterState.userListIndex = root.userList.currentIndex;
diff --git a/src/frontend/greeter/qml/Main.qml b/src/frontend/greeter/qml/Main.qml
index 7a7e725..7e7b361 100644
--- a/src/frontend/greeter/qml/Main.qml
+++ b/src/frontend/greeter/qml/Main.qml
@@ -433,23 +433,9 @@ Item {
         }

         function onLoginFailed() {
-            // The first greeter-visible fingerprint attempt is intentionally
-            // non-interactive. If it times out or no print is enrolled, return
-            // immediately to the normal password UI instead of showing a
-            // spurious generic login failure.
-            if (userListComponent.fingerprintAutoAttemptInFlight) {
-                userListComponent.fingerprintAutoAttemptInFlight = false;
-                userListComponent.fingerprintCancelInFlight = false;
-                userListComponent.pendingPasswordLogin = false;
-                notificationMessage = "";
-                notificationResetTimer.stop();
-
-                footer.enabled = true;
-                mainStack.enabled = true;
-                userListComponent.userList.opacity = 1;
-                return;
-            }
-
+            // Fingerprint has its own terminal/cancellation signal. A generic
+            // LoginFailed therefore belongs to the independent password worker,
+            // even while fingerprint authentication is still in flight.
             notificationMessage = i18nd("plasma_login", "Login Failed");
             // Only failures time out; started here rather than on every change.
             notificationResetTimer.start();
